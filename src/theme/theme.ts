import { createAppTheme } from './themeFactory';

// Default theme instance for backward compatibility or initial rendering
export const Theme = createAppTheme({ mode: 'light' });

export default Theme;
